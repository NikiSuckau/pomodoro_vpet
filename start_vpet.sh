#!/usr/bin/env fish

# Activate the virtual environment named 'pomo'
venv pomo

# Run the Python script
python pomodoro_vpet.py

# How to start this silently:
# To start this script silently, redirect both stdout and stderr to /dev/null:
# ./start_vpet.sh > /dev/null 2>&1
